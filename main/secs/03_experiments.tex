\section{Experiments}

The template includes table helpers from the original paper layout. Use
the table style helper near each table to set compact column spacing and row
height.

\input{tabs/00_example.tex}

As shown in \tabref{tab:example}, symbols such as \texttt{\string\cmark} and
\texttt{\string\xmark} are available for quick comparison tables. For complex
tables, prefer keeping table-specific commands near the table rather than adding
them to the class.
