\begin{figure}[t]
  \centering
  \fbox{%
    \begin{minipage}[c][3.2cm][c]{0.88\linewidth}
      \centering
      \sffamily
      {\Large\bfseries First Visual Module\par}
      \vspace{0.35em}
      {\small Reusable figure wrapper with matching caption text.}
    \end{minipage}%
  }
  \caption{First visual module. The title inside the figure and the caption name
  intentionally match so references such as \figref{fig:first_visual_module} stay
  semantically aligned with the visual.}
  \label{fig:first_visual_module}
\end{figure}
